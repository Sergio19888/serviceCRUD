<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.3.xsd">

    <changeSet id="1" author="mr._robot">
        <!-- Основная таблица заказов -->
        <createTable tableName="orders">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true"/>
            </column>
            <column name="datetime" type="TIMESTAMP"/>
            <column name="orderStatus" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>
            <column name="client_id" type="BIGINT"/>
        </createTable>

        <!-- Связь с клиентом -->
        <addForeignKeyConstraint
                baseTableName="orders"
                baseColumnNames="client_id"
                constraintName="fk_orders_client"
                referencedTableName="client"
                referencedColumnNames="id"/>

        <!-- Таблица для связи orders-products -->
        <createTable tableName="orders_goods">
            <column name="order_id" type="BIGINT"/>
            <column name="good_id" type="BIGINT"/>
        </createTable>

        <addForeignKeyConstraint
                baseTableName="orders_goods"
                baseColumnNames="order_id"
                constraintName="fk_orders_goods_order"
                referencedTableName="orders"
                referencedColumnNames="id"/>

        <addForeignKeyConstraint
                baseTableName="orders_goods"
                baseColumnNames="good_id"
                constraintName="fk_orders_goods_product"
                referencedTableName="products"
                referencedColumnNames="id"/>
    </changeSet>

</databaseChangeLog>